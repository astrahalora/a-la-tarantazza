import "./AddButton.css";

export default function AddButton() {
    return (
        <button 
        type="button"
        className="base-btn add-new-product-btn">
            Add New Product
        </button>
    )
}